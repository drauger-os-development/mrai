TODO List for mrai:
In order of importance
  1. DEBUG DEBUG DEBUG
  2. Add a list installed feature
  3. Add a install from deb feature
  4. Add a check for updates feature
  5. Add a show app information feature (like `apt-get show`)
  6. Add a feature to change the text out of a search or show to inteligently recoignize when an app is being updated in another terminal
  7. Maybe add support for AppImages?
  8. Debug some more
  9. Getting in an existing apt repository or make one for mrai
